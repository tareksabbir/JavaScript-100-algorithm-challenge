// two input a function but it will return specific number

//ex: if a+b = 10  it will return true
//

const checkTen = (x, y) => {
    return ((x + y === 10)||x===10||y===10) ? true : false
}

let result = checkTen(10, 8)
console.log(result)





